import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    modalContainer: {
        paddingTop: '10%'
    },
    closeButton: {
        display: 'flex', 
        flexDirection: 'row', 
        justifyContent: 'flex-start', 
        marginBottom: '2%'
    }
});