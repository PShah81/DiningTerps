import {render, screen} from '@testing-library/react';
import { jest } from '@jest/globals';


describe('Testing Favorites.js',()=>{
    //What To Test
    //The onItemClick and unclickItem feature in the Nutrition Page (FUNCTIONALITY)
    //Actual display of DiningHall Navbar (CONTENT)
    //Actual display of foods in Favorites(CONTNET)
})